apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: posts
spec:
  rules:
    - host: posts-wrichter-dhbw-exercise-1.apps.osd-evalcluster.v0z2.p1.openshiftapps.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: posts
              servicePort: 4000
